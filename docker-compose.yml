services:
  app:
    # Use the image built by GitHub Actions
    image: ghcr.io/fieze/gasstrom:latest
    ports:
      - "4735:4735"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
